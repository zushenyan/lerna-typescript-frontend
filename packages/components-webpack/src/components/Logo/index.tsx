import React, { ReactElement } from 'react';
import logo from '../../assets/images/logo.svg';

console.log(logo);

const Logo = (): ReactElement => <img src={logo} alt="logo" />;

export default Logo;
